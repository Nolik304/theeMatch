{
  "name": "theematch-event-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Server-authoritative event backend for theeMatch (VK Mini App). Uses Vercel serverless functions + Firebase Admin SDK + Firestore.",
  "type": "module",
  "engines": { "node": ">=18" },
  "dependencies": {
    "@google-cloud/firestore": "^7.0.0"
  },
  "scripts": {
    "dev": "vercel dev"
  }
}